dependencies {
    // Swagger
    implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.8.9'

    // Spring Security
    implementation 'org.springframework.boot:spring-boot-starter-security'

    // Flyway
    implementation 'org.flywaydb:flyway-core'
    implementation 'org.flywaydb:flyway-database-postgresql'

    // Integration Test — TestContainers 1.21.3 (Docker Desktop 29.x / API 1.44 호환)
    testImplementation 'org.testcontainers:postgresql:1.21.3'
    testImplementation 'org.testcontainers:junit-jupiter:1.21.3'
    testImplementation 'org.testcontainers:testcontainers:1.21.3'
    testImplementation 'org.wiremock:wiremock-standalone:3.10.0'
}

configurations.all {
    resolutionStrategy.eachDependency { details ->
        if (details.requested.group == 'org.testcontainers') {
            details.useVersion '1.21.3'
        }
    }
}

test {
    useJUnitPlatform {
        if (!project.hasProperty('runIntegration')) {
            excludeTags 'integration'
        }
    }
    if (project.hasProperty('runIntegration')) {
        // Docker Desktop 29.x 최소 API 1.44 요구.
        // TestContainers 번들 shaded docker-java는 env var 대신 system property로 API 버전을 읽음.
        systemProperty "api.version", "1.44"
        environment "DOCKER_API_VERSION", "1.44"
        environment "TESTCONTAINERS_RYUK_DISABLED", "true"
    }
}
